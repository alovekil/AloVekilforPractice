<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AloVekilForPractice" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/RGB_blue</item>
        <item name="colorPrimaryVariant">@color/onlyBlack</item>
        <item name="colorOnPrimary">@color/onlyBlack</item>
        <!-- Status bar color. -->
        <item name="android:windowNoTitle">true</item>

        <item name="android:windowFullscreen">true</item>

        <item name="android:windowActionBar">false</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/RGB_blue</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Button.Blue" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/RGB_blue</item>
    </style>
</resources>